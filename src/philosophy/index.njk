---
title: Philosophy
layout: layout.njk
---
<h2>Philosophy</h2>
<ul>
  {% for post in collections.philosophy %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      — {{ post.date | date("yyyy-MM-dd") }}
    </li>
  {% endfor %}
</ul>