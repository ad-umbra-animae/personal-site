---
title: Psychology
layout: layout.njk
---
<h2>Psychology</h2>
<ul>
  {% for post in collections.psychology %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      — {{ post.date | date("yyyy-MM-dd") }}
    </li>
  {% endfor %}
</ul>